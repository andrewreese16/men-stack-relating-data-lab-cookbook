<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Foods</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>All Foods</h1>
    <a href="/users/<%= user._id %>/foods/new">Add A New Food</a>

    <ul>
      <% pantry.forEach(pantry => { %>
      <li><%= pantry.name %></li>
      <% }) %>
    </ul>
  </body>
</html>
