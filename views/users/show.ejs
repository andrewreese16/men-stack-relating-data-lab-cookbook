<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= user.username %>'s Pantry</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="pantry-container">
      <h1 class="pantry-title"><%= user.username %>'s Pantry</h1>
      <ul class="pantry-list">
        <% if (pantry && pantry.length > 0) { %> <%
        pantry.forEach(function(item) { %>
        <li class="pantry-list-item"><%= item.name %></li>
        <% }) %> <% } else { %>
        <p class="pantry-empty-message">
          This user has no items in their pantry.
        </p>
        <% } %>
      </ul>
      <a href="/users" class="pantry-back-link">Back to Community</a>
    </div>
  </body>
</html>
